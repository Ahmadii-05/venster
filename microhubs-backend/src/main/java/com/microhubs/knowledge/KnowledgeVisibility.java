package com.microhubs.knowledge;

/**
 * Visibility of a KnowledgeItem.
 * PRIVATE: only visible to workspace members (default).
 * PUBLIC: visible in global search to all authenticated users.
 */
public enum KnowledgeVisibility {
    PRIVATE,
    PUBLIC
}
